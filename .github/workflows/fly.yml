name: Fly Deploy

on:
  push:
    branches:
      - main # ou a branch que você usa, ex: master

jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: superfly/fly-pr-review-apps@v1
        with:
          fly_api_token: ${{ secrets.FLY_API_TOKEN }}